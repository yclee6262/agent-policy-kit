{
  "name": "agent-policy-kit",
  "version": "0.2.0",
  "description": "Generate, distribute, and verify repository policies across AI coding agents.",
  "type": "module",
  "bin": {
    "agent-policy-kit": "./bin/agent-policy-kit.js",
    "apk-policy": "./bin/agent-policy-kit.js"
  },
  "files": [
    "bin",
    "examples",
    "CHANGELOG.md",
    "src",
    "templates",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "test": "node --test",
    "lint": "node --check bin/agent-policy-kit.js && node --check src/cli.js && node --check src/core.js"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
